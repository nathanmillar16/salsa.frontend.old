{
  "name": "salsa.frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "husky": {
    "hooks": {
      "prepush": "npm version patch && git add package.json && git add package-lock.json && git commit -m \"Bump version\" --no-verify"
    }
  },
  "dependencies": {
    "husky": "^6.0.0",
    "next": "10.1.3",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
